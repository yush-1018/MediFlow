name: mediflow_mobile
description: Autonomous Healthcare Supply Intelligence Mobile App
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  camera: ^0.10.5
  google_fonts: ^6.1.0
  provider: ^6.0.5
  geolocator: ^10.1.0
  shimmer: ^3.0.0
  firebase_core: ^3.1.0
  firebase_database: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
